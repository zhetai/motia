export const subscribe = ["monitor.redis.status"];

export default async function redisLoggerHandler(input, emit) {
  // Log Redis monitoring events
  // console.log("[Redis Monitor]", {
  //   status: input.status,
  //   timestamp: input.timestamp,
  // });
}

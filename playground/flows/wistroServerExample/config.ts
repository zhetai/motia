// TODO: remove this if we no longer need it
export const config = {
  name: "wistroServerExample",
  description:
    "A simpler workflow that runs on WistroServer with 3 components.",
  version: "1.0.0",
};
